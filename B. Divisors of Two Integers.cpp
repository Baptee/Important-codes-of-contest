#include<bits/stdc++.h>
#include<vector>
using namespace std;
#define lli long long int
#define t               lli t;cin>>t;while(t-->0)
#define ainput  int n;cin>>n;int a[n];for(int i=0;i<n;i++){cin>>a[i];}
#define vec vector<lli>v;
vec
#define avinput  int n;cin>>n;int a[n];for(int i=0;i<n;i++){cin>>a[i];v.push_back(a[i]);}
#define vp  for(lli i=0;i<v.size();i++) {cout<<v[i]<<" ";}cout<<endl;
#define ln cout<<endl;
#define   Y   cout<<"YES"<<endl;
#define   N   cout<<"No"<<endl;
set<lli>s1;
set<lli>s2;
set<string>st2;
vector<lli>v1;
vector<lli>v2;
vector<lli>v3;
vector<lli>v4;
vector<lli>v5;
vector<pair<lli,lli> >v6;
vector<pair<lli,lli> >v7;
vector<string>v10;
vector<string>v8;
vector<char>v9;
vector<double>v11;
list<lli>l;
map<string,lli> m;
//****coding start****


 int main(){
     lli n,x=0,y=0,c=0;cin>>n;int a[n];for(int i=0;i<n;i++){cin>>a[i];v.push_back(a[i]);}
     sort(v.begin(), v.end());
     lli i=v.size()-1;

     for( ;i>0;i--){

            if(v[v.size()-1]%v[i]!=0||v[i]==v[i-1]){
               cout<<v[v.size()-1]<<endl;
    cout<<v[i]<<endl;

                break;
            }}




v.clear();

 }
